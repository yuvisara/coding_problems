#include<bits/stdc++.h>

using namespace std;

int main() {
    string line;
    while(getline(cin, line)) {
        string head = line.substr(0,3);
        string tail = line.substr(4,3);
    }

    return 0;
}
